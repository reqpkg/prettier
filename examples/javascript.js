import abc from 'a'
import { a, b, c } from 'b'
import bcd from 'c'

import abcd from '../d'
import bcde from '../e'

const variableName = abcd({ name: 'abcd', age: 20 })

function exampleFunction() {
  return variableName
}
